{
  "name": "npm-local-time",
  "version": "1.0.0",
  "description": "A small personal npm project just to test out",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel index.html",
    "build": "parcel build index.html"
  },
  "author": "Veronika Gorbatenko (veron-ee)",
  "license": "MIT",
  "homepage": "https://github.com/veron-ee/npm-local-time.git",
  "dependencies": {
    "moment-timezone": "^0.5.45",
    "start": "^5.1.0"
  },
  "devDependencies": {
    "parcel": "^2.12.0"
  }
}